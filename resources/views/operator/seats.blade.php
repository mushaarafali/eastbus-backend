@extends('layouts.app') @section('header','Seat Management') @section('content')<h1 class="page-title">{{$bus->bus_number}} Seat Layout</h1><p class="muted">Green = available/configured, red = disabled by operator.</p><div class="card"><div class="seat-grid">@foreach($seats as $s)<form method="POST" action="{{route('operator.seats.toggle',[$bus,$s])}}">@csrf @method('PATCH')<button class="seat {{$s->is_disabled?'disabled':''}}" style="width:55px" title="Click to toggle">{{$s->seat_number}}</button></form>@endforeach</div></div>@endsection